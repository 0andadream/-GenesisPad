{
  "name": "genesispad",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "vercel-build": "node scripts/build.mjs",
    "postinstall": "cd chart && npm install --omit=dev --no-audit --no-fund || true"
  },
  "dependencies": {
    "@thru/programs": "^0.3.0",
    "@thru/sdk": "^0.3.0",
    "@thru/wallet": "^0.3.0",
    "esbuild": "^0.28.1",
    "gsap": "^3.15.0",
    "lightweight-charts": "^4.2.1",
    "three": "^0.185.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
